
clear all 
close all 
clc 
%--------------------------------------
% Comparing Waveforms With Real Numbers 
%--------------------------------------
    f=??                                      % Hint: this should be a vector or array
    fs=???;                                   % Scalar Value 
    dt=;                                      % Scalar Value 
    t=????                                    % Time Vector 
    A=1;
    
    for N=1:length(f)
        TemplateDataBase(N,:)=A*cos(2*pi*??*t);  % Creates Database where f is specifically indexed by N
    end 